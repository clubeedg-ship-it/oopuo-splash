---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<span class:list={['font-mono text-caption uppercase tracking-[0.08em] text-text-muted', className]}>
  <slot />
</span>
