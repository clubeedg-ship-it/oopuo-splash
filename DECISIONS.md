# OOPUO — Decision Log

| ID | Date | Decision | Rationale |
|---|---|---|---|
| D-028 | 2026-08-01 | Reposition EN as **OOPUO Intelligence — a personal lab**; one accordion component, used once | Supersedes the services-brochure framing for **EN only** (NL/FR/pt-br untouched until EN is approved live). The site's job is credibility that converts into **contracts, not investment** — the operator explicitly rejected an investor framing ("I don't think investors would be putting money on me now. I want that they put jobs and projects for me to do"). Signature trait, and the thing every section must reinforce: *give him a problem that is blocked — legally, structurally, or by privacy — and he finds the configuration that unblocks it.* **Zenithcred is its proof**, and the load-bearing part is not the biofeedback but the consent insight: an employer paying wages holds power over the employee, so consent to biometric measurement is not freely given; unpaid volunteers dissolve the imbalance. Structure follows — the foundation runs the work, a university holds the data, a BV holds the platform IP. Spine: `Arrival → Systems (client proof) → The Lab (own ventures + research) → How I work → Services → Contact`; "The Gap" retired as a section, its argument relocated to the deck intro. Every claim carries a **status tag** (shipped / running / designed); nothing marked designed is presented as running. Private & sovereign work is described **by capability, never by client type** — audience named only as "people who run things", no sector, no client named. **Not positioned toward** advertising defense/weapons capability (commercial reasoning, recorded in spec §10 so it is not reopened later as an oversight): that market runs on clearances and export-control registration rather than marketing, EU Reg. 2021/821 applies to dual-use work from NL, and it would collide with the university ethics review Zenithcred depends on. UI: **one accordion component** (full-height panels, hover/tap expands, activates into a real crawlable URL) used at the **top level only** — nesting it inside a case page was built, rejected by the operator, and removed. Full spec: `docs/superpowers/specs/2026-07-31-oopuo-intelligence-repositioning-design.md`. Nine operator content inputs (spec §9) gate publication, not construction. Next id: **D-029**. |
| D-027 | 2026-06-19 | NL + FR full content parity (split into router pages); resolves OQ-8 for NL/FR | NL and FR now mirror the entire EN page tree (13 pages each): router-mode home (rooms 1+2 + locale-aware hash shim), `/services/` + 4 detail pages, `/studio/`, `/enterprise/`, `/blog/` + 3 posts, `/contact/`. All content translated (NL informal "je", FR formal "vous"); € pricing + EU-AI-Act/Amsterdam/enterprise framing kept (both are the Europe track, D-018); no WhatsApp green. Contact pages use inert `YOUR_NL/FR_HUBSPOT_FORM_ID` placeholders + `hello@oopuo.com` mailto fallback. EN deep pages got the full en/nl/fr/x-default hreflang matrix; `sitemap.xml` regenerated to 40 URLs (13 EN + 13 NL + 13 FR + pt-br home) with per-page language cross-links; the engine locale switcher now maps to the **equivalent page** per locale (`/services/websites/` → `/nl/services/websites/`); mobile-responsive rules added for the switcher + geo-suggest. **pt-br deliberately NOT translated** — it is a separate Brazil strategy (D-018: enterprise/EU-AI-Act/€ never shown to Brazil); its deep content must be Brazil-native (offers, R$ pricing, WhatsApp, parceria), authored not translated. Verified on fresh Playwright (NL/FR journeys, cross-locale hard-load, 0 errors). Next id: D-028. |
| D-026 | 2026-06-18 | Phase 2 EN split shipped; locale chrome injected by the shared engine | EN (Europe) is fully split into real crawlable router pages (executes D-024/D-025 for EN). `index.html` → router-mode home of rooms 1+2 (Arrival + The Gap) with **in-page snap**; a boundary scroll soft-navigates to the next journey stop. Engine gains **side pages** (own `label`/`counter`, not nav stops — enterprise, 4 service details, blog posts, legal) + **multi-room in-page snap** (`pageRooms`/`localIdx`/`goLocal`). **Locale switcher** (EN·NL·FR·PT → locale homes, cross-track hard-load) + dismissible **root geo-suggest** (`navigator.languages`, localStorage) are injected by the shared `engine.js` into the HUD on every page — DRY, so legacy NL/FR/pt-br get them free, no 16-file edit. Old-hash **redirect shim** in EN home `<head>` maps `#03/M.01/2`→`/services/websites/`, `#05/POST.NN`→post, `#enterprise`→`/enterprise/`, `#01/#02` stay on `/`. `sitemap.xml` regenerated. Verified end-to-end on fresh Playwright, 0 console errors. NL/FR/pt-br stay single-page (split per-track next). Home was last (keystone — its rooms 3–6/overlays became the new pages). Next id: D-027. |
| D-001 | 2026-05-14 | Astro + Tailwind + Astro built-in i18n | Zero JS by default fits a static marketing site. Astro islands for WhatsApp wizard. Built-in i18n routing handles 7 locales without middleware. MDX content collections for blog. |
| D-002 | 2026-05-15 | v2 lobby prototype lives in `public/lobby.html` (single file) | Fastest iteration, no build step, no component boundary friction. Production port to Astro pages comes later. |
| D-003 | 2026-05-15 | Three.js + AsciiEffect for the sculpture | Real 3D rotating scene rendered as ASCII gives natural depth shading and continuous rotation across morphs. Lighter than custom shaders, more flexible than pre-rendered animations. |
| D-004 | 2026-05-15 | `EffectComposer` + `UnrealBloomPass` between scene and AsciiEffect | Bloom merges distinct 3D objects into a unified lit field → ASCII chars read as one continuous TUI instead of separate rectangular panels. Patched `renderer.render` delegates to composer with recursion guard. |
| D-005 | 2026-05-16 | `MeshLambertMaterial` (not Phong) + emissive baseline `0x2a2a2a` | Removes specular hotspots that read as "panel" focal points; emissive ensures shadow sides still contribute char density (no hard silhouettes). |
| D-006 | 2026-05-16 | Block-character ramp `' .·:;-+*░▒▓█'`, invert=true | Cellular block chars at the bright end read as "agent units / divs of a system"; dim dots wrap as membrane. Conway's Game of Life aesthetic for the agentic-AI metaphor. |
| D-007 | 2026-05-16 | Three-layer morph: shared curve bow + initial detachment kick + sub-cycle flutter | Coherent direction (shared curve) prevents orbital chaos; initial kick gives "bonds breaking" moment; sub-cycle flutter (period > morph duration) prevents pieces from completing loops → reads as sand-in-wind drift, not constellation. |
| D-008 | 2026-05-16 | Group-level breathing for cluster phase (replaces per-piece wobble) | Whole cluster drifts as one body during the cluster window. Per-piece wobble created orbital chaos; group-level keeps the "one substance" feel. |
| D-009 | 2026-05-17 | Color palette families (cyan default / warm blog), no rainbow rotation | `@property <color>` registration on all `--grad-*` + `--teal` enables 4s smooth transitions between palettes. Narrow `hueGlow` keyframe (±10° at 36s) gives "alive but stays in-family" without rainbow cycling. |
| D-010 | 2026-05-17 | URL hash routing (`#NN` / `#NN/M.NN/N`) instead of porting to Astro | Preserves single-file architecture, adds shareable URLs + browser back/forward + Cmd+R state restoration. ~50 lines of routing code. Astro port deferred until design locks. |
| D-011 | 2026-05-17 | Blog ambience switch is sculpture-color-only, not page-bg | Earlier cream-tint background felt jarring on transition. Now only the sculpture's `--grad-*` + `--teal` shift to warm family; page bg stays `#F0F2F6` across all rooms. |
| D-012 | 2026-05-17 | Asymmetric organic slide via random per-transition tilt + transform-origin | Cheap approximation of "left/right different speed" — random `--room-tilt` (±0.45°) + `--room-origin-x` (20%–80%) per scroll, off-center origin makes one side lift faster. True per-side wave deferred. |
| D-013 | 2026-05-18 | Morph abort via immediate cancel + homePos/homeScale anchors | Replace queue-based morph handling with `abortMorph()` that cancels rAF, snapshots live positions, promotes incoming → current. Permanent `homePos`/`homeScale` prevent position corruption across multiple aborts. |
| D-014 | 2026-05-18 | Favicon square-cropped from logo mark | `viewBox="280 60 460 460"` — square crop of the white mark paths. Wide aspect (1024×544) was letterboxed in square tab-icon space. |
| D-015 | 2026-05-18 | Gospot-format project documentation (CLAUDE.md + PROJECT.md) | Consolidate 7+ scattered state docs into two-file format: CLAUDE.md (hot snapshot) + PROJECT.md (chunked master with §A–§G). CLI retrieval via section anchors. |
| D-016 | 2026-05-18 | Blog reading view as in-DOM slide overlay (hash `#05/POST.NN`) | Posts are `<article class="blog-reader">` panels inside `.stage`, opened by `openPost(n)` with `body.reader-open`. Closes on Esc/back/nav-ball. Keeps single-file architecture; real per-post URLs deferred until v1/v2 merge (OQ-1). |
| D-017 | 2026-05-18 | Canvas is the design; Astro is the product (resolves OQ-1, OQ-3) | `public/lobby.html` is a non-shipping design sketch; the Astro site (`src/pages/`) is what ships, rebuilt pixel-accurate to the canvas. Canvas aesthetic is canonical; conventional v1 page design superseded. Rooms/posts/modules become real crawlable routes; sculpture becomes a persistent View-Transitions island (SEO + seamless nav). English-first launch; other 6 locales post-launch. |
| D-018 | 2026-05-18 | Two markets, two strategies (locale = strategy, not translation) | OOPUO runs two separate GTMs served as non-mixing audience tracks. **Europe** (English; later en-us/nl/fr/rs/zh): enterprise / EU AI Act / Amsterdam — current site framing, kept. **Brazil** (pt-BR): SMB strategy from internal guide — Goiás PMEs, entry offer = automated WhatsApp customer service, expanding to process automation + integrations; "parceria, não produto" partnership; R$3–5k setup + R$350/500/750/mo; LGPD trust. pt-BR is its own copy/offers/pricing, NOT a translation. Enterprise/EU never shown to Brazil; WhatsApp/LGPD/partnership never shown to Europe. Launch sequencing = OQ-7. |
| D-025 | 2026-06-11 | D-024 execution architecture: hybrid app-shell with real URLs (+ policy calls) | Grounded in the 2026-06-11 six-agent audit. Every room/service/post/legal page = a complete static HTML file (full content + meta + OG + JSON-LD in initial HTML — no major AI crawler executes JS); the canvas experience is preserved by a hand-rolled **fetch + DOM-swap + pushState router** (~150–180 lines, `assets/js/router.js`) with the sculpture + HUD mounted OUTSIDE the swap container so the 5.2s morph runs as the page transition (cross-document View Transitions cannot keep WebGL alive — CSS `@view-transition` is fallback crossfade only). The router NEVER soft-navigates across the Europe↔Brazil boundary — D-018 becomes architectural. Policy calls: (1) **blog** = markdown sources in `blog/content/` rendered by a stdlib-only python3 authoring script in `tools/` — sanctioned no-build exception (zero toolchain/deps, output committed; supersedes Keystatic/D-019); (2) **per-locale HubSpot forms** for NL/FR; (3) EN "WhatsApp as support channel" copy (M.02) **whitelisted** in the D-018 rule; (4) **Satoshi dropped from the font canon** (never actually loaded; body = Instrument Sans); (5) **compliance layer mandatory before any real tracking ID** — self-built consent gate, legal pages (incl. statutory `/fr/mentions-legales/`), self-hosted fonts + three.js. Full spec + phases 0–4: PROJECT.md §C. |
| D-024 | 2026-06-03 | Target architecture: multi-page separated static (no build) + media/blog content folders | Restructure the working single-page-per-locale canvas into **separate static HTML pages** (one per section/route), keeping the visual canon and **no build step**. Add: shared `assets/` (css + sculpture js + fonts/logos), a `media/` folder for images (blog banners, OG), and a `blog/` folder where each post is a content file (yaml or markdown: title, date, body, banner-image filename) + its page. Each section/post becomes a real crawlable URL. Deploy `public_html/` (or a `dist/`) to Hostinger — still no build. Next agent makes a state snapshot + plans this to deployment (see `HANDOFF.md`). Also still open: geo/language auto-routing at the root. |
| D-023 | 2026-06-03 | Meta Pixel + HubSpot form + Meetings installed; `site/` → `public_html/` | Meta Pixel base code in `<head>` of all 4 locales (placeholder `YOUR_META_PIXEL_ID`; console shows `Invalid PixelID: null` until set). HubSpot contact on Europe (EN/NL/FR): **Meetings** "Book a free call" → placeholder `meetings-eu1.hubspot.com/oopuo` (set real slug). **Form** embedded on EN room 6 — portal 148607612, region eu1, real form `2fef7ceb-b34c-4792-9a0d-1a2d618767b9`. ⚠️ Learnings: it is a **new-builder** form → use the **new embed** (`https://js-eu1.hsforms.net/forms/embed/148607612.js` + `<div class="hs-form-frame" data-form-id data-portal-id data-region>`), NOT legacy `hbspt.forms.create` v2 (which silently renders nothing for new forms); the new-embed iframe is `height:100%` → give it `min-height`; and it **does not render on http://localhost — only on the live https domain**, so test on oopuo.com. Loaded only when the contact room becomes visible. Form is on EN only → replicate to /nl/ + /fr/. Brazil keeps WhatsApp. Web root renamed `site/`→`public_html/`. |
| D-022 | 2026-06-02 | Europe contact via HubSpot Meetings (free); NL/FR Europe pages shipped | NL (`site/nl/`) + FR (`site/fr/`) Europe pages added (lean: 6 rooms, no sub-rooms/blog/enterprise — EN-only at launch). Europe contact ("Book a free call", EN/NL/FR room 6) wired to **HubSpot Meetings** — portal **148607612**, region **eu1** (from connected HubSpot MCP). Placeholder meeting link `https://meetings-eu1.hubspot.com/oopuo` — operator replaces the slug with their real free Meeting link. "Send a message" stays `mailto:hello@oopuo.com` (can become a HubSpot form share link later). Brazil keeps WhatsApp (brand colors, never green). hreflang + sitemap cover all 4 locales. |
| D-021 | 2026-06-02 | Remove Astro entirely — pure static, no build, no Node (completes D-020) | Deleted `src/`, `public/`, `astro.config.mjs`, `package.json`, `package-lock.json`, `node_modules/`, `dist/`, `tsconfig.json`, eslint/prettier config. Repo is now hand-authored static HTML in `site/` only — no build step, no npm. Preview = `Static Site` launch config (python http.server on `site/`, port 4330). The canvas/design source is now `site/index.html` itself (no separate `public/lobby.html`). Three.js via CDN importmap (no bundler). Launch scope trimmed to **EN + pt-BR** (the two real strategies); NL/FR deferred to fast-follow. |
| D-020 | 2026-06-02 | Ship a static, no-build site (supersedes D-017/D-019 Astro build) | Production = hand-authored static HTML, **no build step**, uploaded to Hostinger (`site/`). Core experience = the canvas single-page (snap-scroll + persistent morph + legibility scrim) per locale: `/` (Europe EN), `/nl/`, `/fr/` (Europe), `/pt-br/` (Brazil SMB — different content); real static `/blog/<slug>.html` pages for SEO. Shared `assets/sculpture.js` + `styles.css` to be extracted. Astro work parked (committed, revertible). Why: the canvas already delivers the loved, working experience; the Astro multi-route rebuild lost snap-scroll + scrim + polish and felt messy; Brazil motion is outbound (low SEO need); no-build = trivial deploy. Preview via `Static Site` launch config (port 4330). |
| D-019 | 2026-06-01 | Launch scope: EN/pt-BR/NL/FR at launch; static set-and-forget; Hostinger + HubSpot + Keystatic | All chosen locales ship at launch — **EN, pt-BR, NL, FR** (resolves OQ-3; en-us/rs/zh deferred). Brazil/pt-BR ships at launch with the Europe track (resolves OQ-7). After upload the site is **frozen** — only ongoing change is posting new blog pages. Host = **Hostinger** (static `dist/` deploy via CI; resolves OQ-6). Contact + scheduling = **HubSpot** embeds (forms + meetings; replaces Cal.com; exact setup TBD — tentatively resolves OQ-4); Brazil leads with WhatsApp deep link. Blog = **Keystatic** local mode — operator edits posts locally, rebuilds `dist/`, uploads (resolves OQ-5). Deploy = manual `dist/` upload (no CI; operator builds + uploads). Hostinger serves static only — all integrations are client-side embeds. |
